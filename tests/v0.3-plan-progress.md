# v0.3 plan 进度跟踪

> 跟踪 `docs/v0.3-plan.md` 各阶段执行 / 验收 / W 系裁决。
> 沿用 v0.2-plan-progress.md / v0.2.x-hotfix-progress.md 同款治理风格。

---

## 阶段 0: 基线确认

**执行日期**:2026-05-25
**执行者**:Claude Code(按 `docs/v0.3-plan.md` §3.5)
**结果**:Review 3 条全 YES,smoke 12/12 PASS,进阶段 1

### §3.5.2 前置检查清单(4 命令)

```text
git: OK
tests/: OK
docs/: OK
scripts/: OK
corpus/: OK
scripts/ingest.py: OK
scripts/search.py: OK
scripts/smoke_test.py: OK
docs/v0.2-plan.md: OK
tests/v0.2-plan-progress.md: OK
tests/查询记录.md: OK

# ✅ smoke_test 12/12 PASS
```

### §3.5.4 Review 检查清单

| # | 检查项 | 结果 |
|---|---|---|
| 1 | `.git/ + tests/ + docs/ + scripts/ + corpus/` 全在 | ✅ YES |
| 2 | scripts/ 三脚本 + docs/v0.2-plan.md + tests/v0.2-plan-progress.md + tests/查询记录.md 全在 | ✅ YES |
| 3 | `python scripts/smoke_test.py` 报告 `12/12 PASS` | ✅ YES |

**裁决**:PASS,进阶段 1。

---

## 阶段 1: schema + 契约文档

**执行日期**:2026-05-25
**执行者**:Claude Code(按 `docs/v0.3-plan.md` §4.3)
**结果**:步骤 1.1-1.5 完成,Review 9/9 全 YES;smoke 12/12 不掉(阶段 1 不动 smoke,阶段 2 一起扩到 15/15)

### 关键事实快照

1. **Layer 6 三层校验集成**:`_validate_tier_field` 加到 ingest.py 末尾(line ~669 附近),由 `_validate_plan_item_paths` 在 Layer 5 后调用;`execute_plan` 读 `plan.get('plan_schema_version')` 顶层字段传入 validator
2. **frontmatter 3 字段注入位置**:`process_file_with_explicit_target` line 799 附近,`frontmatter = dict(item.get("frontmatter") or {})` 后立即用 `setdefault` 写 `kb_tier / kb_default_search / family_key` 三字段(用 setdefault 避免覆盖 AI 在 plan 里手填的同名字段)
3. **CLAUDE.md §6 Step E**:在现有 Step D 后追加,5 类 tier 判定要点 + 三 bucket 适用范围 + family_key Windows 非法字符约束
4. **v0.2 旧 plan 兼容性**:阶段 1 端到端实测 — 无 `plan_schema_version` + 无 `tier` 字段的 plan 跑通,默认走 normal + 自动注入 `kb_tier: normal` + `kb_default_search: true`,**不报错不警告**

### §4.5 Review 检查清单 9/9 PASS

| # | 检查项 | 结果 | 证据 |
|---|---|---|---|
| 1 | CLAUDE.md §6 Step E 已加,覆盖 5 tier + 约束 + family_key + 三 bucket | ✅ YES | `grep -n "Step E" CLAUDE.md` 命中;含 canonical/normal/working/versions/assets 全 5 类 + projects/areas/resources 三 bucket + family_key Windows 非法字符约束 |
| 2 | `_validate_tier_field` 实现 Layer 6.1/6.2/6.3 | ✅ YES | scripts/ingest.py 含 `_validate_tier_field` 函数 + 6.1 必填判定 / 6.2 类型+非空+白名单 / 6.3 family_key + WIN_INVALID 校验 |
| 3 | v0.3 新 plan(`plan_schema_version: v0.3`)缺 tier → 拒(Layer 6.1) | ✅ YES | 手工 13 case 验证:"v0.3 缺 tier" → `ValueError: v0.3 plan_schema_version requires tier field (missing)` |
| 4 | v0.2 旧 plan(无 `plan_schema_version`)缺 tier → 默认 normal,**不报错** | ✅ YES | 端到端实测:无 `plan_schema_version` + 无 `tier` 的 plan exit 0,落地 frontmatter 含 `kb_tier: normal` + `kb_default_search: true` |
| 5 | tier 白名单拒非法值(Layer 6.2) | ✅ YES | 手工 case:`tier=evil` → `ValueError: tier 'evil' not in whitelist`;canonical/normal/working/versions/assets 5 类全过 |
| 6 | `tier=versions` 时 `family_key` 必填实测拒空(Layer 6.3) | ✅ YES | 手工 case:`tier=versions` 缺 family_key + family_key="   " 都拒 `ValueError: tier='versions' requires non-empty family_key` |
| 7 | `family_key="<bad|name>"` 拒 Windows 非法字符(Layer 6.3 新增) | ✅ YES | 手工 case:`<bad\|name>` 拒 `family_key contains Windows-invalid chars ['<', '>', '\|']`;`a/b` 同样拒(含 /);中文 `中文_家族` 过 |
| 8 | frontmatter 3 字段在新 ingest 后正确注入 .md / .stub.md | ✅ YES | 端到端 5 tier × .md 实测:canonical/normal → `kb_default_search: true`;working/assets/versions → `false`;versions 额外写 `family_key`;v0.2 原 type/tags 不破坏 |
| 9 | `tests/v0.3-plan-progress.md` 已新建 | ✅ YES | 本文件 |

### 顺手验证(超 §4.5 范围但相关)

| 顺手项 | 结果 |
|---|---|
| smoke_test 12/12 PASS(阶段 1 改动未破 v0.2.3 基线) | ✅ |
| `_validate_plan_item_paths` 签名兼容(默认 `plan_schema_version=None` 保留向后调用) | ✅ |
| `kb_default_search: true / false` YAML 序列化正确(非 `True`/`False` 字符串) | ✅ |
| `setdefault` 不覆盖 AI 在 plan.frontmatter 手填的同名字段(防 AI 覆盖意图) | ✅ |
| `plan_schema_version != 'v0.3'` 走 v0.2 兼容(包括未来 v0.4 plan,留 v0.4 处理) | ✅ 设计已防御 |

### 阶段 1 改动文件清单

| 文件 | 改动 |
|---|---|
| `scripts/ingest.py` | + `ALLOWED_TIERS` 常量;+ `_validate_tier_field` 函数(6.1/6.2/6.3 三层);+ `_validate_plan_item_paths` 签名加 `plan_schema_version` 参数 + Layer 5 后调用 Layer 6;+ `execute_plan` 读顶层 `plan_schema_version` 传入;+ `process_file_with_explicit_target` frontmatter 注入加 3 字段 |
| `CLAUDE.md` §6 | + `#### Step E: tier 判定`(适用 projects + areas + resources 三 bucket,5 类判定要点 + 约束 + family_key Windows 非法字符) |
| `tests/v0.3-plan-progress.md` | 新建(本文件) |

### 已知约束 / 未在阶段 1 处理(留阶段 2/3/4)

- **plan §5.3 步骤 2.3(frontmatter 注入加 3 字段)实际在阶段 1 完成**:跟 §4.3 步骤 1.3(schema 定义)不可分;阶段 2 §5.5 Review 11 改为"回归验证 frontmatter 在 `.shelved/` 物理路径下仍正确注入",不是"实现 frontmatter 注入"
- `_build_target_dir` **暂未支持 tier 路由**(本阶段 frontmatter 注入了,但 working/versions/assets 仍落到现有 5 子目录,跟 canonical/normal 一样)— 阶段 2 步骤 2.1 实现 `.shelved/` 物理路由
- smoke_test **未扩 13/14/15**(阶段 1 不改 smoke,跟阶段 2 一起扩到 15/15)
- `search.py --deep` 未加(阶段 3)
- migration helper / E11 fixture / README / migration guide 未做(阶段 4)

### W 系观察点

#### W-v0.3-阶段1-W1: 测试残留误入 commit

**现象**:阶段 1 首次 commit 误带入 `corpus/02-areas/产品方案库/` 内 2 个测试残留文件:

- `smoke_broken.docx.md`
- `v03_fm_smoke.txt`

**处置**:已 `git reset --soft` 清理 + 重提,最终 main 上的 commit 干净。

**防御规则**(沿用 v0.2 W 系机制):阶段 2/3/4 每次 commit 前必须 `git status` 扫一遍 `corpus/` 路径,测试残留必须清理。

**升级条件**:

- 阶段 2/3/4 再次出现类似残留 → 升级,把测试用 fixture 命名规则统一为 `corpus/.testtmp/` 子目录
- 持续 3+ 阶段都干净 → W1 关闭

**当前状态**:观察中,沿用防御规则即可。

### 裁决

请 Hugin 通读阶段 1 输出,给出三种判断之一:

- **PASS** → 进阶段 2(ingest.py 物理实现 + `.shelved/` 结构)
- **CONDITIONAL_PASS** → 继续推进,某条 W 系留观察
- **FAIL** → 阶段 1 返工,具体指出哪条不达标

**Hugin 裁决**(2026-05-25):**PASS** + 加 W-v0.3-阶段1-W1(测试残留误入)防御规则,进阶段 2。

---

## 阶段 2: ingest.py 物理实现 + .shelved/ 结构

**执行日期**:2026-05-25
**执行者**:Claude Code(按 `docs/v0.3-plan.md` §5.3)
**结果**:步骤 2.1-2.6 完成,Review §5.5 14/14 全 YES;smoke 12/12 → 15/15 PASS

### 关键事实快照

1. **`_build_target_dir` 三 bucket × 4 路由分支**:projects / areas / resources 三 bucket × canonical-normal / working-assets / versions / 未知 raise 四分支;archives 单独分支(忽略 tier)
2. **multi-level subdir split** + **defense-in-depth**:`item["target_subdir"].strip("/").split("/")` 处理多级 subdir + 末尾 `Path.resolve().relative_to(CORPUS)` 拦截 escape
3. **B 分散物理结构实测**:`corpus/02-areas/<subdir>/.shelved/<tier>/` 与 `corpus/03-resources/<subdir>/.shelved/versions/<family_key>/` 都落地正确
4. **archives 忽略 tier 实测**:plan 给 `tier=working` 但 `bucket=04-archives` → 落 `corpus/04-archives/<project>/` 直接根,**不走 .shelved**
5. **smoke_test 12 → 15**:加 [13/15] 非法 tier 拒(双反向断言)+ [14/15] tier=working 端到端 + [15/15] family_key Windows 非法字符拒(三向反向断言)
6. **W-v0.3-阶段1-W1 防御生效**:smoke [14/15] + 端到端测试用虚构项目名 `_v03_*_`,测完 `shutil.rmtree` 清理 corpus 残留

### §5.5 Review 检查清单 14/14 PASS

| # | 检查项 | 结果 | 证据 |
|---|---|---|---|
| 1 | tier=working 实测落 `项目/.shelved/working/<subdir>/` | ✅ YES | e2e `_v03_phase2_e2e_/.shelved/working/99-其他/working.md` 存在 |
| 2 | tier=versions 实测落 `项目/.shelved/versions/<family_key>/` | ✅ YES | e2e `_v03_phase2_e2e_/.shelved/versions/总体方案_md/v1.md` 存在 |
| 3 | tier=assets 实测落 `项目/.shelved/assets/<subdir>/` | ✅ YES | e2e `_v03_phase2_e2e_/.shelved/assets/05-附图/shot.md` 存在 |
| 4 | areas bucket tier=assets 实测落 `corpus/02-areas/<subdir>/.shelved/assets/` | ✅ YES | e2e `corpus/02-areas/_v03_areas_test_/.shelved/assets/a_assets.md` 存在 |
| 5 | resources bucket tier=versions 实测落 `corpus/03-resources/<subdir>/.shelved/versions/<family_key>/` | ✅ YES | e2e `corpus/03-resources/_v03_resources_test_/.shelved/versions/国标_v1/r_v1.md` 存在 |
| 6 | archives bucket 不走 tier(plan 给了也忽略) | ✅ YES | e2e plan 含 `bucket=04-archives + tier=working` → 实落 `corpus/04-archives/_v03_phase2_e2e_/arc.md`(不进 .shelved) |
| 7 | tier=canonical/normal 行为完全等价 v0.2.3(向后兼容) | ✅ YES | e2e canonical 落 `01-方案/`(同 v0.2.3 行为);v0.2 旧 plan 端到端 Test A 通过(无 plan_schema_version + 无 tier → 落 `01-方案/legacy.md` + 自动注入 kb_tier=normal) |
| 8 | Layer 6.1 v0.3 新 plan 缺 tier 拒 | ✅ YES | 阶段 1 已验证(13 case 手工);smoke [13/15] 间接覆盖(给非法 tier 拒因明确含 'not in whitelist') |
| 9 | Layer 6.2 白名单拒非法 tier(`evil` / `unknown`) | ✅ YES | smoke [13/15] PASS:tier=`evil` 拒 'not in whitelist',反向断言 `missing required field` 不在 |
| 10 | Layer 6.3 versions 时 family_key 必填实测;含 Windows 非法字符实测拒 | ✅ YES | smoke [15/15] PASS:`family_key=bad<name\|here` 拒 'Windows-invalid chars',双反向断言(不是 Layer 1 / 不是 Layer 6.2) |
| 11 | frontmatter 3 字段在 .md 和 .stub.md 都正确注入 | ✅ YES | smoke [14/15] 验证 `kb_tier: working` + `kb_default_search: false` 在 `.shelved/working/` 落地 .md;阶段 1 5 tier 测试覆盖 .md 路径,本阶段不破 |
| 12 | 旧 v0.2.x routing_plan(无 plan_schema_version + 无 tier)重跑行为不变 | ✅ YES | e2e Test A:exit 0 + 落 `01-方案/`(老路径)+ 注入 kb_tier=normal + kb_default_search=true + 旧字段 type=old_v02 保留 |
| 13 | `_build_target_dir` resolve + relative_to(CORPUS) 防御实测(构造 escape attempt → 拒) | ✅ YES | e2e Test B:`target_filename=../../../../../../etc/passwd` → Layer 5 路径穿越校验在 _build_target_dir 之前拒(`ERROR_INVALID_PLAN_ITEM` + 'path traversal' / `..` 关键词);`_build_target_dir` 末尾 resolve+relative_to 是双保险,Layer 5 是第一道防线 |
| 14 | smoke 15/15 PASS | ✅ YES | `python scripts/smoke_test.py` → `# ✅ smoke_test 15/15 PASS` |

### 阶段 2 改动文件清单

| 文件 | 改动 |
|---|---|
| `scripts/ingest.py` `_build_target_dir` | **改写**:三 bucket × 4 路由分支(canonical-normal / working-assets / versions / archives 忽略 tier);multi-level subdir split(`.strip("/").split("/")`);末尾 resolve + relative_to(CORPUS) defense-in-depth 沿用 |
| `scripts/smoke_test.py` | + [13/15] 非法 tier 拒(双反向断言);+ [14/15] tier=working 端到端 .shelved/working/(含虚构项目清理);+ [15/15] family_key Windows 非法字符拒(三反向断言);docstring 同步 v0.2.3/12 → v0.3/15;[N/12] → [N/15] 编号同步;main() fn 列表加 3 行 |
| `tests/v0.3-plan-progress.md` | 追加阶段 2 段(本段) |

### 顺手验证(超 §5.5 范围但相关)

| 顺手项 | 结果 |
|---|---|
| 8 落地路径端到端 ALL PASS(projects 5 tier + areas + resources + archives) | ✅ |
| `_validate_plan_item_paths` 在 Layer 5 路径穿越校验早于 `_build_target_dir`(Layer 5 拒 escape 后不会进 Layer 6 / _build_target_dir) | ✅ |
| W-v0.3-阶段1-W1 防御生效:测试虚构项目名 `_v03_*_`,测完 shutil.rmtree 清理;corpus/ 干净 | ✅ |
| smoke [14/15] 跑完 corpus 内 `_v03_smoke_working_` 项目自动清理 | ✅ |
| 端到端 8 路径测试完虚构项目 4 个目录全清理 + 端到端 Test A/B 虚构项目 2 个全清理 | ✅ |

### 已知约束 / 未在阶段 2 处理(留阶段 3/4)

- `search.py --deep` 未加(阶段 3,默认 search 不会排除 `.shelved/**`)
- migration helper / E11 fixture / README / migration guide 未做(阶段 4)
- CLAUDE.md §5.5 fixtures 路径约定 / §5.6 运维操作速查 未加 .shelved 排除示例(阶段 3 / 4 一起做)

### 裁决

请 Hugin 通读阶段 2 输出,给出三种判断之一:

- **PASS** → 进阶段 3(search.py `--deep` flag + smoke 16/16)
- **CONDITIONAL_PASS** → 继续推进,某条 W 系留观察
- **FAIL** → 阶段 2 返工,具体指出哪条不达标
