# v0.2.2 hotfix 执行进度

> 跟踪 v0.2.1 release(2026-05-21 03:56 UTC)后 Codex 第二轮审查触发的 3 条问题。
> 分支:`hotfix/v0.2.2`;新增 release 工作流约束:**tag + push 之前先停下来让 Codex 再审一轮**。

**执行日期**:2026-05-21(v0.2.1 release 当天 < 24h)
**执行者**:Claude Code(按 Hugin web 端裁决拆分 C-1 + C-2 + C-3 + smoke 扩充)

---

## 修复清单(每条独立 commit,从上到下顺序)

| # | 类别 | 主题 | 改动文件 | commit SHA | 验收 |
|---|---|---|---|---|---|
| C-1 | Codex 中 | execute_plan schema 补 `target_subdir` 必填 + 01-projects 时 `target_project` 必填 | `scripts/ingest.py` | `a189201` | 4 case 全过:缺 subdir 拒 / 01-projects 缺 project 拒 / 02-areas 缺 project 通过(null 合法)/ 01-projects 全字段通过 |
| C-2 | Codex 中低 | plan JSON 读取 `encoding="utf-8"` → `"utf-8-sig"` 兼容 BOM | `scripts/ingest.py` | `a189201`(与 C-1 合并 commit)| 2 case 全过:无 BOM + 有 BOM(EF BB BF 前缀)JSON parse 都成功 |
| C-3 | Codex 低 | `.gitignore` 加 `.claude/`(Claude Code working state 隔离) | `.gitignore` | `97ef949` | `git status -- .claude/` → clean(scheduled_tasks.lock 不再 untracked) |
| smoke + 顺手修 | — | smoke_test 扩充 6/6 + `inject_frontmatter` 读 utf-8-sig 顺手修 | `scripts/smoke_test.py` / `scripts/ingest.py` | `d930065` | 全局 Python 实测 6/6 PASS |

---

## C-1 + C-2 合并 commit 设计取舍

按 Hugin 指示"每条独立 commit",但 C-1 + C-2 都改 `scripts/ingest.py` 同一函数边界(`execute_plan` 开头几行)+ schema 检查重构,**单 commit 改动连贯,拆开反而割裂语义**。决定合并 commit,在 message 内明示两条修复。

C-3 是独立文件改动,独立 commit。smoke 扩充与 utf-8-sig 顺手修都属于"验收 / 顺手"范畴,合 1 commit。

---

## 全仓 grep 扫雷(沿用 v0.2.1 防御规则)

### utf-8 grep

```bash
grep -rn 'encoding="utf-8"' --include='*.py' scripts/
```

命中 20+ 处。逐一评估:

| 文件 | 行 | 类型 | 处置 |
|---|---|---|---|
| `archive_check.py:16` | `sys.stdout.reconfigure` | 输出流 | 不动(不涉及 BOM) |
| `archive_check.py:65` | `write_text` 写报告 | 写产物 | 不动(我们自己生成,不带 BOM 是 by-design) |
| `ingest.py:49/50` | `sys.stdout/stderr.reconfigure` | 输出流 | 不动 |
| `ingest.py:121` | `write_text(json.dumps(...))` 写 routing_request | 写产物 | 不动 |
| `ingest.py:137` | `read_text` 读 .md(inject_frontmatter)| **读外部 hand-edit** | **顺手改 utf-8-sig**(Windows Notepad 默认存 BOM)|
| `ingest.py:159/164` | `write_text` 写 .md | 写产物 | 不动 |
| `ingest.py:435/443` | `INGEST_LOG` 读写 jsonl | 我们自己 append | 不动 |
| `ingest.py:672+` 各 `stub_path.write_text` | 写 stub.md | 写产物 | 不动 |
| `ingest.py:743` `md_path.write_text(md_text)` | markitdown 转 .md 写 | 写产物 | 不动 |

**结论**:只 `inject_frontmatter` 这一点读外部可能 hand-edit 的 .md,顺手改 `utf-8-sig`。其他读 / 写产物都不需要改。

### REQUIRED_FIELDS / required_fields grep

```bash
grep -rn 'REQUIRED_FIELDS\|required_fields' --include='*.py'
```

唯一命中:`scripts/ingest.py:535`(本次新加)+ `.venv/Lib/.../python_message.py`(protobuf 源码,无关)。**无其他遗漏 schema 校验点**。

---

## smoke_test 实际输出(6/6 PASS)

```text
# smoke_test.py — v0.2.2 minimal smoke test 6/6 (Python: C:\...\Python311\python.exe)

[1/6] search.py --scope all --help (install 烟测调用参数)... ✓
[2/6] ingest.py scan-only corpus/.fixtures/E8_scope_routing/... ✓
[3/6] execute-plan 拒绝 3 个 malformed plan(P0-2 路径边界)... ✓ (3 cases)
[4/6] import docx + yaml + markitdown... ✓
[5/6] execute-plan 拒绝缺 target_subdir 的 plan(C-1 schema)... ✓
[6/6] execute-plan 读 UTF-8 BOM plan(C-2 BOM 兼容)... ✓

# ✅ smoke_test 6/6 PASS
```

---

## 干净 .venv 烟测

```text
=== .venv 复用(v0.2.1 release 时已建,Python 3.11.9 + 全套依赖)===

=== .venv 内跑 smoke_test 完整 6/6 ===
[1/6] ... ✓
[2/6] ... ✓
[3/6] ... ✓ (3 cases)
[4/6] ... ✓
[5/6] ... ✓  ← C-1 验收
[6/6] ... ✓  ← C-2 验收

# ✅ smoke_test 6/6 PASS

=== .venv 内 install.bat 等价烟测命令 ===
.venv\Scripts\python.exe scripts\search.py --scope all --terms "__install_smoke_test_zzzz__"
  → exit 0 ✓
```

---

## 待 Codex 再审 + Hugin 通知后流程(本 progress 写完时尚未发生)

1. `git checkout main`
2. `git merge --no-ff hotfix/v0.2.2` — **STOP**(不 tag,不 push,不 release)
3. 输出 4 commits SHA + smoke log + .venv log 给 Hugin
4. Hugin 把 main HEAD 或 hotfix 分支 commits 给 Codex 再审
5. Codex 审过后,Hugin 通知继续:
   - `git tag -a v0.2.2 -m "..."`
   - `git push origin main`
   - `git push origin v0.2.2`
   - `gh release create v0.2.2 ...`(沿用 v0.2.1 流程)

---

## 致谢 / release 工作流硬约束

v0.2.0 release(2026-05-21 01:50 UTC)→ Codex 第一轮(< 24h)→ v0.2.1 hotfix(2026-05-21 03:56 UTC)→ Codex 第二轮(同一 working session,~1h 内)→ v0.2.2 hotfix(本次)

**Codex 持续审查 + 持续响应是本项目的 release 工作流硬约束**。从 v0.2.2 起明确:**每次 release 前过一遍 Codex 静态审查,通过才发版本**;merge main 后必停下来让 Codex 看一眼,不再"merge 完直接 tag + push"。

这是开源项目应有的"被审" + "可验证"工作流闭环。

---

## Codex 第三轮反馈处置: README schema 同步

**反馈时间**:2026-05-21(v0.2.2 主修 merge 后立即)
**反馈数量**:1 条文档修复

### 反馈内容

v0.2.2 主修把代码层 `REQUIRED_FIELDS` 从 `{src_abs, target_bucket, target_filename}` 升级为 `{src_abs, target_bucket, target_subdir, target_filename}`,但 `README.md` line 182 routing_plan.json 样例段的"每个 item 必填字段说明"未同步,**仍写老的 3 字段必填**。文档与代码不一致。

### 修复(commit `bcda67c`)

| 维度 | 修订前 | 修订后 |
|---|---|---|
| 必填字段列表 | `src_abs / target_bucket / target_filename` | `src_abs / target_bucket / target_subdir / target_filename / frontmatter / ai_reason` |
| target_project 条件 | "仅 01-projects 必填" | "仅在 `target_bucket = 01-projects` 时必填,其他 bucket(02-areas / 03-resources / 04-archives)允许为 null" |
| frontmatter.project 描述 | 保留(非 projects 类应为 null) | 保留(同) |
| 中英文同步 | 中文有,英文无 routing_plan 段 | **英文版补完整 routing_plan.json sample 段**(P0-7 当时只改中文,顺势补) |

### 验收 grep(本次新增)

```bash
grep -n "src_abs.*target_bucket.*target_subdir.*target_filename" README.md README.en.md
# 两文件命中行都包含完整 6 字段 + target_project 条件描述
```

输出:

```text
README.md:182    含 src_abs / target_bucket / target_subdir / target_filename / 
                 frontmatter / ai_reason 完整字段 ✓
README.en.md:183 同上,英文版同步 ✓
```

### 顺手发现(超 Codex 第三轮反馈范围)

P0-7 加 routing_plan.json 样例段时只改中文 README.md,英文 README.en.md **没同步**。Codex 第三轮没明确点出英文缺,但中英双语对齐是文档代际同步的隐含约束(沿用 v0.2.0 阶段 6 review #2 "README 中英双语对齐")。本次顺势补完英文版,**两文件结构一致**。

### Codex 第四轮复审

Hugin 跑 Codex 复审,验收范围限定:
1. README.md + README.en.md 两文件 schema 描述与 `scripts/ingest.py REQUIRED_FIELDS` 完全一致
2. 中英文双语描述一致(字段名 / 必填条件)
3. 没引入新的不一致或拼写错误

复审通过后,Hugin 通知继续 tag + push + release。

---

## v0.2.2 hotfix 完整 commit 链(本地 main 领先 origin/main,等 Codex 第四轮通过后 push)

| # | SHA | 主题 |
|---|---|---|
| 1 | `a189201` | C-1 + C-2: plan schema 补 target_subdir/target_project + utf-8-sig BOM |
| 2 | `97ef949` | C-3: .gitignore 加 .claude/ |
| 3 | `d930065` | smoke_test 扩充 6/6 + inject_frontmatter 读 utf-8-sig 顺手修 |
| 4 | `a5a3402` | docs: tests/v0.2.2-hotfix-progress.md(3 修复 + smoke + .venv) |
| 5 | `f9586ef` | merge hotfix/v0.2.2 → main(`--no-ff`) |
| 6 | `bcda67c` | docs: README routing_plan schema 同步代码必填字段(Codex 3rd 反馈) |
| 7 | `896e589` | docs: progress.md 追加 Codex 3rd 处置段 + 4th 复审验收点 |
| 8 | `27b0e68` | Codex 4th 反馈:REQUIRED_FIELDS 收口 6 字段 + smoke_test 8/8 + docstring 同步 |
| 9 | `e022544` | docs: progress.md 追加 Codex 4th 处置段 + 5th 终审验收点 |
| 10 | `d4cc68e` | Codex 5th: validate_plan_schema 三层校验 + smoke_test 路径边界真实覆盖 + docstring |
| 11 | (本次)| docs: progress.md 追加 Codex 5th 处置段 + 6th 复审验收点 |

---

## Codex 第五轮反馈处置:validate_plan_schema 系统性升级到 3 层校验

**反馈时间**:2026-05-21(Codex 第四轮通过后立即)
**反馈数量**:4 条 + Hugin 裁决系统性升级(打补丁式扩字段 → 完整 3 层校验)

### 背景(诚实记录)

Codex 5 轮累计 19 条问题,其中 `REQUIRED_FIELDS` 主题反复迭代:

| 轮次 | 修复 | 实际暴露的根本设计缺口 |
|---|---|---|
| v0.2.1 P0-2 | 加 src_abs / target_bucket / target_filename(key 存在) | 只校验 key 存在,未校验非空、未校验类型 |
| v0.2.2 C-1 | 加 target_subdir + 01-projects 时 target_project | 同上 |
| v0.2.2 Codex-4th | 加 frontmatter + ai_reason | 同上 |
| **v0.2.2 Codex-5th(本轮)** | **升到 3 层** | **systematic fix:key 存在 + 类型 + 非空** |

**反思**:前 3 轮都是打补丁式扩字段,**每轮都是新一轮 Codex 找出来的下一个漏字段**。Hugin 第五轮裁决系统性升级,**不只修 Codex 找出的 2 条字段,把所有 6 字段一次性完整校验**,避免下轮 Codex 再发现 ai_reason 空串 / target_filename 非 str / src_abs 非绝对路径等同类问题。

### 修复(commit `d4cc68e`)

#### 1. `REQUIRED_FIELDS` 集合 → `REQUIRED_FIELDS_SCHEMA` 字典

```python
REQUIRED_FIELDS_SCHEMA = {
    "src_abs":         {"type": str,  "non_empty": True},
    "target_bucket":   {"type": str,  "non_empty": True},
    "target_subdir":   {"type": str,  "non_empty": True},
    "target_filename": {"type": str,  "non_empty": True},
    "frontmatter":     {"type": dict, "non_empty": False},  # 空 dict {} 合法
    "ai_reason":       {"type": str,  "non_empty": True},
}
```

`REQUIRED_FIELDS = set(REQUIRED_FIELDS_SCHEMA.keys())` 别名保留(progress.md 历史引用兼容)。

#### 2. `_validate_plan_item_paths` 重构为 5 层

| Layer | 内容 | 错误信息特征 |
|---|---|---|
| 1 | key 存在 | `missing required field: X` |
| 2 | 类型正确 | `field 'X' must be {type}, got {actual_type}: {value!r}` |
| 3 | 非空(仅 str + non_empty=True) | `field 'X' must not be empty string (got: {value!r})` |
| 4 | 字段特殊规则(src_abs 绝对路径 / bucket 白名单 / 01-projects target_project) | `src_abs must be absolute path` / `target_bucket 'X' not in whitelist` / `01-projects bucket requires non-empty target_project` |
| 5 | 路径穿越(沿用 P0-2) | `X contains '..' path traversal segment` / `X contains absolute path prefix (Windows drive)` / `X starts with path separator` / `X contains path separator (must be single-level)` |

#### 3. smoke_test 8/8 → 10/10

| Assert | 修订 | 说明 |
|---|---|---|
| [3/10] | **升级**:6 字段补齐 + 错误信息匹配 `path traversal` / `not in whitelist` / `absolute path`;断言 `"missing required field" not in stdout` | 确保是被 Layer 5 路径校验拒,不是被 Layer 1 缺字段拒(原 8/8 版的"路径边界"测试其实被 schema 缺字段拦下,是假覆盖) |
| [9/10] **新增** | frontmatter 非 dict(None / str / list)→ "must be dict" | Layer 2 类型校验覆盖 |
| [10/10] **新增** | target_subdir / ai_reason 空串 + strip 后空 → "empty string" | Layer 3 非空校验覆盖 |
| docstring | "4 assert" / "6/6" / "8/8" 全清理 | `grep '4 assert' / '6/6' / '8/8' scripts/smoke_test.py = 0 命中` ✓ |

### 手工 16 边界 case 验收(直调 `_validate_plan_item_paths`)

| 维度 | Case | 期望 | 结果 |
|---|---|---|---|
| Layer 2 类型 | `frontmatter=None` | 拒(must be dict)| ✓ |
| Layer 2 类型 | `frontmatter="string"` | 拒(must be dict)| ✓ |
| Layer 2 类型 | `frontmatter=[]` | 拒(must be dict)| ✓ |
| Layer 2 类型 | `frontmatter={}` | 通过(空 dict by-design)| ✓ |
| Layer 2 类型 | `frontmatter={"type":"方案"}` | 通过 | ✓ |
| Layer 3 非空 | `ai_reason=""` | 拒(empty string)| ✓ |
| Layer 3 非空 | `ai_reason="   "` | 拒(strip 后空)| ✓ |
| Layer 2 类型 | `ai_reason=None` | 拒(must be str)| ✓ |
| Layer 3 非空 | `target_subdir=""` | 拒 | ✓ |
| Layer 3 非空 | `target_filename=""` | 拒 | ✓ |
| Layer 3 非空 | `src_abs=""` | 拒 | ✓ |
| Layer 4 绝对路径 | `src_abs="relative/path.md"` | 拒(must be absolute)| ✓ |
| 综合 | 合法 6 字段 | 通过 | ✓ |
| Layer 4 条件必填 | 01-projects `target_project=None` | 拒 | ✓ |
| Layer 4 条件必填 | 01-projects `target_project="   "` | 拒(strip 后空)| ✓ |
| Layer 4 条件必填 | 01-projects `target_project="X"` | 通过 | ✓ |

**Total: 16 cases, fail = 0** ✓

### smoke_test 10/10 实际输出

```text
# smoke_test.py — v0.2.2 minimal smoke test 10/10 (Python: ...\Python311\python.exe)

[1/10] search.py --scope all --help (install 烟测调用参数)... ✓
[2/10] ingest.py scan-only corpus/.fixtures/E8_scope_routing/... ✓
[3/10] execute-plan 真实拒 3 个路径边界 malformed plan(Layer 5 路径穿越)... ✓ (3 cases, real path boundary)
[4/10] import docx + yaml + markitdown... ✓
[5/10] execute-plan 拒绝缺 target_subdir 的 plan(schema Layer 1 key 存在)... ✓
[6/10] execute-plan 读 UTF-8 BOM plan(BOM 兼容)... ✓
[7/10] execute-plan 拒绝缺 frontmatter 的 plan(schema Layer 1 key 存在)... ✓
[8/10] execute-plan 拒绝缺 ai_reason 的 plan(schema Layer 1 key 存在)... ✓
[9/10] execute-plan 拒绝 frontmatter 非 dict 的 plan(Layer 2 类型校验)... ✓ (3 type cases)
[10/10] execute-plan 拒绝 target_subdir / ai_reason 空字符串(Layer 3 非空)... ✓ (3 empty-string cases)

# ✅ smoke_test 10/10 PASS
```

### Codex 第六轮复审验收点

范围限定:**只看 commit `d4cc68e` + 本次 progress.md 追加段**

1. `validate_plan_schema` 现在是**完整 3 层校验**(key 存在 + 类型正确 + 非空),不是打补丁式只修 Codex 提到的字段 ✓
2. 所有 6 字段 + target_project(条件必填)的所有"空"和"非法类型"组合行为符合预期 — 16 手工 case + 3 smoke type case + 3 smoke 非空 case = 22 case 全过 ✓
3. smoke_test `[3/10]` 路径边界 3 case 现在**真的测路径边界**(6 字段补齐,只让路径字段坏),错误信息含 `path traversal` / `not in whitelist` / `absolute path` 路径相关关键词,断言 `"missing required field" not in stdout` ✓
4. smoke_test 新增 `[9/10]` `[10/10]` 类型校验和非空校验覆盖完整 ✓
5. docstring 同步(grep '4 assert' / '6/6' / '8/8' = 0 命中)✓
6. `inject_frontmatter` 已不可能撞 "frontmatter 非 dict 调 `.items()` 崩"(因为 validate 阶段 Layer 2 已拒)✓
7. 没引入新问题:只改 `_validate_plan_item_paths` + smoke_test;其他 ingest 流程不变;`REQUIRED_FIELDS` 别名保留向后兼容 ✓

第六轮通过后,Hugin 通知继续 tag + push + release。

---

## Codex 第四轮反馈处置:REQUIRED_FIELDS 6 字段收口 + smoke_test 8/8

**反馈时间**:2026-05-21(Codex 第三轮通过后立即)
**反馈数量**:2 条(中 + 低)

### 4th-1 (中):代码 vs 文档不一致 — Codex 实测 plan 缺 frontmatter / ai_reason 仍 DRY_RUN_TEXT 通过

**根因**:v0.2.2 主修 C-1 把 `REQUIRED_FIELDS` 从 `{src_abs, target_bucket, target_filename}` 升到 `{... + target_subdir}`(4 字段),但 README/README.en/CLAUDE.md §6.3 声明 6 字段必填(含 frontmatter + ai_reason)+ `process_file_with_explicit_target` docstring(line 621)也写 6 字段。**代码常量 vs 代码自身 docstring vs 三份文档全不一致**(Codex 第三轮只盯文档同步,这次实测代码才发现常量也漏)。

**修复**(commit `27b0e68`):

```python
REQUIRED_FIELDS = {
    "src_abs",
    "target_bucket",
    "target_subdir",
    "target_filename",
    "frontmatter",      # ← 4th 新加
    "ai_reason",        # ← 4th 新加
}
```

**校验语义明示**(REQUIRED_FIELDS 上方注释):

- 只看 key 是否存在,**不强制非 null**:`frontmatter=null` 或 `frontmatter={}` 都通过(下游 `item.get("frontmatter") or {}` 把 null 视同 {})
- `ai_reason=null` 也通过(AI 推理字符串可空)
- 这跟 `target_project=null` 在非 projects bucket 合法的设计**一致**(null = "明示无值",不是"字段未提供")

### 4th-2 (低):smoke_test.py docstring 引用 v0.2.1 + assert 数已过时

**修复**:
- docstring 顶部 `v0.2.1 / 4 assert` → `v0.2.2 / 8 assert`
- 内联注释清理所有 `v0.2.1` 字面引用(改为更通用措辞,如"本脚本不引入 pytest 重型测试栈 / v0.2.2 起 8 个核心 assert")
- 验收 grep:`grep v0.2.1 scripts/smoke_test.py` = **0 命中**(严苛标准)

### smoke_test 扩 6/6 → 8/8

| Assert | 修订 |
|---|---|
| [5/8] | 测试 plan 补齐 6 字段后,精确测"只缺 target_subdir"(否则会同时缺 frontmatter + ai_reason,拒因模糊) |
| [6/8] | BOM plan 加 `ai_reason` 字段避免被 4th-1 修后误伤为缺字段 |
| [7/8] **新** | 拒绝缺 frontmatter 的 plan(4th-1 验收) |
| [8/8] **新** | 拒绝缺 ai_reason 的 plan(4th-1 验收) |

### 手工 4 case 验收(超 smoke 范围,直调 `_validate_plan_item_paths`)

| Case | 期望 | 结果 |
|---|---|---|
| 缺 frontmatter | ERROR_INVALID_PLAN_ITEM | ✓ 拒:`plan item 缺必填字段 ['frontmatter']` |
| 缺 ai_reason | ERROR_INVALID_PLAN_ITEM | ✓ 拒:`plan item 缺必填字段 ['ai_reason']` |
| frontmatter=null | 允许(key 在 + 下游 `or {}` 容错) | ✓ 通过 |
| 合法 6 字段 | 通过 | ✓ 通过 |

### smoke_test 8/8 实际输出

```text
# smoke_test.py — v0.2.2 minimal smoke test 8/8 (Python: ...\Python311\python.exe)

[1/8] search.py --scope all --help (install 烟测调用参数)... ✓
[2/8] ingest.py scan-only corpus/.fixtures/E8_scope_routing/... ✓
[3/8] execute-plan 拒绝 3 个 malformed plan(P0-2 路径边界)... ✓ (3 cases)
[4/8] import docx + yaml + markitdown... ✓
[5/8] execute-plan 拒绝缺 target_subdir 的 plan(C-1 schema)... ✓
[6/8] execute-plan 读 UTF-8 BOM plan(C-2 BOM 兼容)... ✓
[7/8] execute-plan 拒绝缺 frontmatter 的 plan(Codex-4th-1 REQUIRED_FIELDS 6 字段)... ✓
[8/8] execute-plan 拒绝缺 ai_reason 的 plan(Codex-4th-1 REQUIRED_FIELDS 6 字段)... ✓

# ✅ smoke_test 8/8 PASS
```

### Codex 第五轮复审(终审)验收点

范围限定:**只看 commit `27b0e68` + 本次 progress.md 追加段**

1. `scripts/ingest.py REQUIRED_FIELDS` 现含 6 字段,与 README/README.en/CLAUDE.md 声明的必填字段完全一致 ✓
2. 缺 `frontmatter` / `ai_reason` 的 plan 实测被拒(ERROR_INVALID_PLAN_ITEM,不崩溃)✓
3. smoke_test 8/8 PASS,新增 2 assert 逻辑健壮 ✓
4. smoke_test.py docstring 已同步 v0.2.2(grep v0.2.1 = 0 命中)✓
5. 没引入新问题(只动 REQUIRED_FIELDS 常量 + smoke_test;校验语义、其他 ingest 流程不变)

第五轮通过后,Hugin 通知继续 tag + push + release。
