# 视频-合成-100色彩切换-5分钟.mp4 — vision 转写

## 元数据头

- 文件名: 视频-合成-100色彩切换-5分钟.mp4
- 类型: 视频抽帧
- 源文件相对路径: corpus/.fixtures/V4_medium_video/视频-合成-100色彩切换-5分钟.mp4
- vision_ingest: 2026-05-13
- 模型版本: Claude Code 内置 Claude(claude-opus-4-7[1m])
- 时长: 300.0 秒(5 分钟)
- 抽帧总数: 50(命中 5-30 min 档上限 50)
- 抽帧策略: 双层降级链触底 — scene 0.3 抽 0 帧 → scene 0.1 抽 0 帧 → 按时长上限均匀采样(fps=1/6,每 6 秒 1 帧,共 50 帧);源视频 100 个 3 秒色块切换,但 ffmpeg scene 算法对纯色突变 SAD 值未达阈值

## 整体描述

### 帧序列叙事

一段 5 分钟的合成测试视频,源帧序列为 100 张 HSV 均匀分布的纯色块(每张持续 3 秒)。色彩从红→橙→黄→绿→青→蓝→紫→洋红连续循环 ≈ 1.5 圈,在抽出的 50 帧中表现为周期性色相轮转。
本视频不含任何文字、图标、动效或场景结构,纯粹用于 V4 评估 vision 流程的路径 B 双层抽帧逻辑(scene 0.3 → scene 0.1 → 时长上限均匀采样)+ 时间戳保留 + 上限触发后的均匀采样正确性。

由于内容是纯色无信息帧,**本视频不具备语义检索价值**;V4 测试目标是验证"双层算法 0 帧降级链 + 上限 50 命中"的正确性,不是语义命中率。

## 关键元素清单

按帧周期组织(50 帧均匀分布于 [00:00, 04:54]):

- 帧序列含 50 个不同色相的纯色块,色相按 HSV 圈周期 0→360° 连续 ≈ 1.5 圈
- 每帧固定饱和度 1.0、亮度 0.85
- 无文字、无图标、无对象、无线条
- 帧间相邻色相差 ≈ 7.2°(360°/50)

## 文字识别(OCR)

无文字。

## 关系/流向

非示意类,无关系图。本视频是合成色块序列,无节点/连线/流向。

## 不确定部分

- vision 转写出于评估成本,仅对 5 张代表帧(00:00 / 01:12 / 02:24 / 03:36 / 04:54)做了详细 Read,其余 45 帧的色相记录是按 HSV 算法**推断**而非逐帧 Read 验证
- ffmpeg scene 算法在纯色全幅切换场景下未触发 0.3 / 0.1 阈值的根本原因(SAD 算法对宏块边缘敏感,纯色宏块差值低)未深查;真实业务视频(含纹理/对象/文字)scene 算法应表现正常
- yuv420p 色彩空间转换可能让原 HSV 设计的色相在 vision 看到时略偏

## 帧明细列表

### [00:00] 帧 1(代表帧,已 Read 验证)

- 全幅鲜红色(约 RGB 217,0,0,HSV h=0°)
- 无文字、无图标

### [01:12] 帧 13(代表帧,已 Read 验证)

- 全幅亮黄绿色(约 RGB 135,217,0,HSV h≈87°)
- 无文字、无图标

### [02:24] 帧 25(代表帧,已 Read 验证)

- 全幅青绿色(约 RGB 0,217,184,HSV h≈171°)
- 无文字、无图标

### [03:36] 帧 37(代表帧,已 Read 验证)

- 全幅深蓝紫色(约 RGB 60,0,217,HSV h≈257°)
- 无文字、无图标

### [04:54] 帧 50(代表帧,已 Read 验证)

- 全幅鲜红色(约 RGB 217,0,0,HSV h=355°,回环色相)
- 无文字、无图标

### 其余 45 帧(算法推断,未逐帧 Read 验证)

- 时间戳依次为 [00:06] / [00:12] / [00:18] / ... / [04:48],间隔 6 秒
- 色相按 HSV 算法连续轮转,饱和度 1.0,亮度 0.85
- 内容性质同代表帧:全幅纯色,无文字、无图标
